import React from 'react';
import ReactDOM from 'react-dom/client';
import ControlerBudget from './components/ControlerBudget';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
      <ControlerBudget />
  </React.StrictMode>
);

